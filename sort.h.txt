sort.h

#ifndef SORT_H
#define SORT_H

#include "student.h"

typedef enum {
    SORT_BY_ID,
    SORT_BY_NAME,
    SORT_BY_AVERAGE
} SortKey;

void sort_students(Student *students, int count, SortKey key);

#endif /* SORT_H */